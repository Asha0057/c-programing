#include<stdio.h>
#include<stdlib.h>
struct node()
{
   int data;
   struct *node
}*head,*temp,*newnode;
void main()
{
    int n,i;
    printf("enter elements");
    scanf("%d",n);
    for(i=0;i<=n;i++)
    {
        newnode=(struct node*)malloc((sizeof(struct node));
        scanf("%d",newnode->data);
        newnode->next=NULL;

    }
}
