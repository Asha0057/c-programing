#include<stdio.h>
#include<string.h>
int main()
{

    char a[10],b[10];
    gets(a);
    int k=0,i,j;
    for(i=0;a[i]!='\0';i++);
    {
        1
    }
        for(j=i-1;j>=0;j--)
    {
        b[k]=a[j];
        k++;
    }
    b[k]='\0';
    printf("%s",b);
    return 0;
}
